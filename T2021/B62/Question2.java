package T2021.B62;

public class Question2 {
    public static boolean search(int[][] mat, int num) {
        // if (mat[mat.length][0] < num) {
        //     return false;
        // }

        // int leftRow = 0, leftCol = 0, rightRow = mat.length, rightCol = 0;

        // boolean cubeStart;
        // boolean cubeEnd;
        // boolean isLeftStart = true, isLeftEnd = false, isRightStart = false, isRightEnd = true;

        // if (leftCol % 2 == 0) {
        //     if (leftRow % 2 == 0) {
        //         isLeftStart = true;
        //         isLeftEnd = false;
        //     } else {
        //         isLeftEnd = true;
        //         isLeftStart = false;
        //     }
        // }

        // if (rightCol % 2 == 0) {
        //     if (rightRow % 2 == 0) {
        //         isRightStart = true;
        //         isRightEnd = false;
        //     } else {
        //         isRightEnd = true;
        //         isRightStart = false;
        //     }
        // }

        // int middleRow = 0, middleCol = 0;

        // while (leftRow <= rightRow && leftCol <= rightCol) {
        //     if (mat[middleRow][middleCol] == num) {
        //         return true;
        //     }

        //     if (isLeftStart && isRightEnd) {

        //     }

        // }
        return false;
    }
}
